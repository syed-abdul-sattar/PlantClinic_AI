# Datasets

The training and evaluation datasets are intentionally not included in this GitHub repository.

The project uses large image datasets for model training and validation. These datasets should be distributed separately because of their size and because each source dataset has its own licensing and redistribution conditions.

The GitHub repository contains the source code and configuration required to run the inference pipeline once the required model files and datasets are obtained separately.
